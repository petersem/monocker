version: '3.4'

services:
  monocker:
    container_name: monocker
    image: monocker
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
      TELEGRAM_ID: '1472584417:AAFW1zbGk17i8kGeTLAByLxmwkaBScWhqvo'
      TELEGRAM_CHAT_ID: '-448418280'
      LABEL_DISABLE: 'false'
      LABEL_ENABLE: 'false'
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - 3300:3000
